<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lz.student">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".reglog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TeacherMenu"></activity>
        <activity android:name=".StudentMenu"></activity>
        <activity android:name=".AddInfo"></activity>
        <activity android:name=".AllInfo"></activity>
        <activity android:name=".ChangeInfo"></activity>
        <activity android:name=".DeleteInfo"></activity>
        <activity android:name=".DetailInfo"></activity>
        <activity android:name=".FindChange"></activity>
        <activity android:name=".FindInfo"></activity>
        <activity android:name=".ShowFindInfo"></activity>
    </application>

</manifest>